{
  "$comment": "Copy this file (e.g. to config/my-pull.json, which is gitignored) and fill it in, then pass it to Invoke-EDiscoveryExport.ps1 -ConfigFile. See docs/authentication-setup.md for the auth block.",

  "case": {
    "name": "Matter-1234 - Keyword Collection",
    "description": "Keyword pull for <matter/claim>, <date range>, <who>."
  },

  "search": {
    "name": "Keyword Search",
    "keywords": ["contract dispute"],
    "startDate": "2022-01-01",
    "endDate": "2022-12-31",
    "contentQuery": null,
    "$comment": "Each keyword element becomes an OR'd KQL group, e.g. ['contract dispute'] -> (contract dispute). Set contentQuery to raw KQL to override keywords+dates entirely."
  },

  "mailboxes": [
    "alice@example.com",
    "bob@example.com",
    "shared-legal@example.com",
    "shared-hr@example.com"
  ],

  "members": ["admin@example.com"],

  "export": {
    "format": "pst",
    "singlePst": true,
    "criteria": "searchHits",
    "location": "responsiveLocations",
    "includeReport": true,
    "includePartiallyIndexed": false,
    "$comment": "singlePst:false adds 'splitSource' for one PST per mailbox. format: pst or msg."
  },

  "output": {
    "dir": "exports/matter-1234"
  },

  "auth": {
    "mode": "app-only",
    "appId": "00000000-0000-0000-0000-000000000000",
    "tenantId": "00000000-0000-0000-0000-000000000000",
    "certThumbprint": "ABCDEF0123456789ABCDEF0123456789ABCDEF01",
    "certPath": null,
    "certPasswordEnv": null,
    "downloadResourceAppId": "b26e684c-5068-4120-a679-64a5d2c909d9",
    "$comment": "mode: app-only | interactive. Fill appId/tenantId with your Entra app + tenant, certThumbprint with your cert in the store (or set certPath to a .pfx + certPasswordEnv naming an env var with its password). downloadResourceAppId is the Microsoft first-party Purview eDiscovery download resource - the same GUID for every tenant; leave it as-is. Full setup: docs/authentication-setup.md."
  }
}
